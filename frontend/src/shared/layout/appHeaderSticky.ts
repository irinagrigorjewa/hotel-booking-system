/**
 * Single-row AppHeader Toolbar minHeight (px).
 * HotelFilters sticky `top` must match — do not wrap the header into a taller bar on xs.
 */
export const APP_HEADER_STICKY_TOP_PX = { xs: 52, sm: 56 } as const
